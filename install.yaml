name: elasticvue

dependencies:
  - ddev/ddev-elasticsearch

# list of files and directories listed that are copied into project .ddev directory
project_files:
- docker-compose.elasticvue.yaml

global_files:
  - commands/host/elasticvue
